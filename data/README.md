# Training Data

The synthetic training datasets used for HoneyMind are not included in this repository due to their size.

Datasets can be regenerated using the scripts in:

scripts/

The final model was trained using a large synthetic dataset of attacker terminal interactions and Linux system responses.
